# Gemfile
# activesupport 7.1.1 is broken, and cocoapods dont have a pinned
# dependency on activesupport, so we need to pin it here until it's fixed.
gem "activesupport", "= 7.0.8"
gem "cocoapods", "= 1.13.0"
